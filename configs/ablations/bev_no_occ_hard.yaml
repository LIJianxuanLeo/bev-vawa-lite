# Cross-distribution eval for the BEV-no_occ variant:
# compose the channel ablation (occ disabled) with the harder env
# sampling used by pib_nav_hard.yaml. Used only at eval time.
inherit: ./bev_no_occ.yaml
env:
  room_size_m: [7.0, 10.0]
  n_obstacles: [6, 10]
  obstacle_size_m: [0.25, 1.4]
